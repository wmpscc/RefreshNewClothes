<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.wmpscc.refreshnewclothes">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- if you want to load images from the internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="design_width"
            android:value="1080">
        </meta-data>
        <meta-data
            android:name="design_height"
            android:value="1920">
        </meta-data>

        <activity android:name=".Activity.DesignerInfoActivity">
        </activity>
        <activity android:name=".Activity.SearchActivity">
        </activity>
        <activity android:name=".Activity.DealListActivity">
        </activity>
        <activity android:name=".Activity.KuaiDiInfoActivity">
        </activity>
        <activity android:name=".Activity.MessageListActivity">
        </activity>
        <activity android:name=".Activity.SettingActivity">
        </activity>
        <activity android:name=".Activity.CommodityDetailsActivity">
        </activity>
        <activity android:name=".UserBaseInfoActivity">
        </activity>
        <activity android:name=".Activity.LoginActivity">
        </activity>
        <activity android:name=".Activity.LogonActivity">
        </activity>
    </application>

</manifest>